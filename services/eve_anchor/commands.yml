commands:
  - name: help
    description: Describe available commands.
    command_options:
      - name:
        description:
        kind: string
        required: false
  - name: config
    description: Configure sever level settings.
    command_options:
      - name: system_name
        description:
        kind: string
        required: false